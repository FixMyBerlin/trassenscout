name: Deploy App to Ionos
on:
  workflow_call:
    inputs:
      ENVIRONMENT:
        required: true
        type: string
      DEPLOY_WORKFLOW_FILE:
        required: true
        type: string

jobs:
  diff_app:
    uses: ./.github/workflows/diff-last-run.yml
    secrets: inherit
    with:
      ENVIRONMENT: ${{ inputs.ENVIRONMENT }}
      CHECK_DIFF_LOCATIONS: src/ prisma/ public/ emails/ docker/ package.json bun.lock vite.config.mts prisma.config.ts .dockerignore
      ENV_FINGERPRINT_SCOPE: app
      DEPLOY_WORKFLOW_FILE: ${{ inputs.DEPLOY_WORKFLOW_FILE }}

  build_app:
    runs-on: ubuntu-latest
    needs: diff_app
    if: ${{ needs.diff_app.outputs.CHANGES == 'true' }}
    timeout-minutes: 30
    environment:
      name: ${{ inputs.ENVIRONMENT }}
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: "AWS: Configure credentials"
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: eu-central-1

      - name: Setup buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to ECR
        run: aws ecr get-login-password --region eu-central-1 | docker login --username AWS --password-stdin ${{ vars.ECR_REGISTRY }}

      - name: Build and push app image
        uses: docker/build-push-action@v6
        with:
          # Path context: use the checked-out tree so .dockerignore applies the same way as locally.
          context: .
          file: ./docker/app/Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ vars.ECR_REGISTRY }}/trassenscout/app:${{ github.sha }}
            ${{ vars.ECR_REGISTRY }}/trassenscout/app:${{ inputs.ENVIRONMENT }}-latest
          build-args: |
            VITE_APP_ENV=${{ inputs.ENVIRONMENT }}
            VITE_APP_ORIGIN=${{ vars.APP_ORIGIN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Prepare app environment fingerprint artifact
        run: |
          bun .github/scripts/prepare-env-fingerprint-cache.ts \
            --cache-key "${{ needs.diff_app.outputs.env_fingerprint_cache_key }}" \
            --artifact-name "app-${{ inputs.ENVIRONMENT }}"

      - name: Cache app environment fingerprint
        if: ${{ needs.diff_app.outputs.env_fingerprint_cache_key != '' }}
        uses: actions/cache@v6
        with:
          path: .ci/env-fingerprints
          key: ${{ needs.diff_app.outputs.env_fingerprint_cache_key }}
