# Moved — Trassenscout auth patterns

FMC/Trassenscout-specific auth wiring (manual cookies, `endpointAuth`, route protection, OSM sign-in) lives in the **local** skill:

**Skill:** `trassenscout-auth`  
**Doc:** [`.agents/skills/trassenscout-auth/references/auth.md`](../../trassenscout-auth/references/auth.md)

This file remains as a pointer when the portable `tanstack-start-auth` skill is synced from upstream.

For `betterAuth()` configuration, plugins, and adapters, continue using [better-auth-config.md](better-auth-config.md) in this skill.
