# npm configuration for Next.js 14 + Blitz
# https://docs.npmjs.com/cli/v11/configuring-npm/npmrc

# Save exact versions (no ^ or ~) for reproducible installs
save-exact=true

# Use legacy peer dependency resolution to avoid conflicts
# This is especially important for Blitz and Next.js compatibility
legacy-peer-deps=true

# Note: npm hoists all packages by default, so hoisting patterns
# (public-hoist-pattern) are not needed. Those were pnpm-specific options.
