# IMAP Listener Service - Environment Variables Example
# Kopiere diese Datei zu .env und passe die Werte an

# IMAP Verbindung
IMAP_HOST=imap.migadu.com
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=your-email@example.com
IMAP_PASSWORD=your-password

# API Konfiguration
# For local development: use host.docker.internal to reach localhost from Docker
# For production: use 'http://app:3000/api/process-project-record-email'
TS_API_WEBHOOK_URL=http://host.docker.internal:6173/api/process-project-record-email
TS_API_KEY=ok

# Lucky Cloud
# Bitwarden "luckycloud.de API"
LUCKY_CLOUD_TOKEN=123
