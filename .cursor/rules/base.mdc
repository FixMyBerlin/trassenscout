---
description: Whenever Prisma and the database is used.
alwaysApply: false
---

## Prisma

- Do not use prisma directly, but via blitz instead. For example, to create a migration use `npm run migrate:create`
